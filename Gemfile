source 'https://rubygems.org'

# Specify your gem's local dependencies here
path '../../internal' do
  # gem 'some_local_gem'
end
path '../../world' do
  # gem 'some_local_gem'
end

# Specify your gem's remote dependencies in the gemspec file
gemspec
